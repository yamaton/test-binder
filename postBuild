npm install -g npm

# jupyterlab-lsp fails to find bash-language-server if installed with '-g'.
# Related? https://github.com/jupyter-lsp/jupyterlab-lsp/issues/181
npm install bash-language-server

condax install -c conda-forge -c bioconda samtools
condax install -c conda-forge -c bioconda seqkit

echo 'export babatest=keketest' >> ~/.bashrc