#!/bin/bash
export SHELL=/usr/bin/zsh

# - Replaces the back end: NotebookApp --> ServerApp
# - Set user's default directory to $HOME/work, separating it from $HOME
exec $(echo "$@" --ServerApp.root_dir=work | sed 's/notebook/lab/g')
